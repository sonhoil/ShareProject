import React, { useEffect, useState } from 'react';
import KakaoLoginButton from '../../components/Buttons/Login/KakaoLoginButton';

const Login: React.FC = () => {
  // 사용할 state들과 useEffect 훅이 있다면 이곳에 작성하세요.

  return (
    <div>
      <input />
      <input />
      <KakaoLoginButton />
    </div>
  );
};

export default Login;